@model IEnumerable<TourismWeb.Models.PostTag>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Post)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tag)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Post.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tag.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { postId = item.PostId, tagId = item.TagId }) |
                @Html.ActionLink("Details", "Details", new { postId = item.PostId, tagId = item.TagId}) |
                @Html.ActionLink("Delete", "Delete", new { postId = item.PostId, tagId = item.TagId })
            </td>
        </tr>
}
    </tbody>
</table>
